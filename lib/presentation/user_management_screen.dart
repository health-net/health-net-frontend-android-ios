import 'package:flutter/material.dart';

class UserManagementScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child:Text("Caracalla doveva morire sul rogo ma il governo ci nasconde i vaccini quindi moriremo tutti per combustione spontanea")
      ),
    );
  }
  
}